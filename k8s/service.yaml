apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: monitoring-app
  labels: 
    app: webapp
spec:
  type: NodePort
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007
      name: http
